{
  "main": "build/index.js",
  "author": "cathiecode",
  "license": "CC0-1.0",
  "private": "true",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --outfile=build/index.js --sourcemap --bundle",
    "dev": "concurrently 'esbuild src/index.ts --outfile=build/index.js --sourcemap --bundle --watch' 'nodemon'"
  }
}
